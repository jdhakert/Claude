# ---- API (api/) ----
NODE_ENV=development
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info
# Postgres connection (required for migrations, seed, and DB-backed routes)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/barready
# Comma-separated allowed CORS origins
CORS_ORIGINS=http://localhost:5173

# ---- Client (client/) ----
VITE_API_BASE_URL=http://localhost:3000
